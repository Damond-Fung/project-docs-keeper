---
name: "project-docs-keeper"
displayName: "Project Docs Keeper"
allowed-tools: Bash(node *)
description: "Auto-updates 3 local project docs (talk, dev log, deployment manual). Invoke after each development change, release/deploy, or when user asks to summarize the project process."
---

# Project Docs Keeper

这个 skill 用来把"开发过程"沉淀成 3 份可持续维护的文档，并在每次开发、合并、发布、部署之后自动增量更新：

- 项目宣讲稿（可用于论坛稿/公众号稿，能复现、能让新手快速理解）
- 项目开发过程记录（问题与解决方案库，帮助后续快速避坑）
- 项目部署操作手册（把最终包部署到生产环境的逐步操作）

## 触发场景（什么时候调用）

- 完成一次需求/修复/重构后，需要把变更沉淀为项目过程记录与宣讲稿更新
- 完成一次发版/部署/回滚/变更配置后，需要更新部署手册
- 新同学接手项目，需要基于宣讲稿快速理解并能复现

## 必要输入（缺什么就先补什么）

- 代码变更来源（优先级从高到低）：
  - git diff / commit（若项目使用 git）
  - 变更文件清单与关键片段（若未使用 git）
  - 构建/部署命令与执行输出（用于还原可复现步骤）
- 环境信息：dev / test / prod 的差异（域名、配置注入方式、依赖、权限、网络、监控）

## 配置（环境变量）

- `PDK_ENV`：`dev | test | prod`（默认 `dev`）
- `PDK_OUT_DIR`：本地文档输出目录（默认 `./project_docs`）

## 执行入口（脚本）

本 skill 默认提供一个可落地的执行层：会在 3 份本地 Markdown 文档中维护一个"自动更新区"，实现：
- 实时刷新"最新更新"
- 追加"历史更新"（幂等去重）
- 目标文档缺失时自动创建（可关闭）

```bash
node ./scripts/project-docs-keeper.mjs
```

常用参数：
- `--env dev|test|prod`
- `--out-dir <path>`
- `--create-missing true|false`
- `--change-id <id>`
- `--summary <text>`
- `--watch`
- `--watch-root <path>`
- `--watch-debounce-ms <number>`
- `--install-git-hooks`
- `--uninstall-git-hooks`

## 自动触发（可选）

支持两种自动触发方式：

1. `git hook`：提交/合并/切分支后自动更新
2. `watch`：保存文件后自动更新

`git hook` 安装：

```bash
node ./scripts/project-docs-keeper.mjs --install-git-hooks
```

`git hook` 卸载：

```bash
node ./scripts/project-docs-keeper.mjs --uninstall-git-hooks
```

`watch` 启动：

```bash
node ./scripts/project-docs-keeper.mjs --watch
```

可选指定监听根目录和防抖时间：

```bash
node ./scripts/project-docs-keeper.mjs --watch --watch-root . --watch-debounce-ms 1200
```

说明：
- `watch` 会自动忽略 `project_docs/`、`.git/`、`node_modules/`，避免文档回写导致自触发死循环
- `watch` 适合开发中"保存即更新"
- `git hook` 适合提交后形成更稳定的里程碑记录
- `watch` 触发时，会把"触发来源文件路径"和"影响模块"写入《项目开发过程记录》

## 自动更新区（定位标记）

脚本会在每份本地 Markdown 内维护以下可搜索的定位标记，用于稳定增量更新：
- `<!-- PDK_AUTO_SECTION -->`：自动更新区存在性标记
- `<!-- PDK_LATEST_UPDATE -->`：最新更新占位标记（会被复写）
- `<!-- PDK_HISTORY_START -->`：历史更新锚点（新条目插到它后面）
- `<!-- PDK_CHANGE_ID:<id> -->`：历史更新条目的去重键（避免重复写入）

## 核心工作流（每次调用都按这个顺序走）

### 1) 收集"本次增量"上下文

- 读取本次需求的目标、验收点、影响范围
- 获取变更证据（优先顺序）：
  - git diff（工作区/暂存区/对应 commit）
  - 关键命令执行记录（构建/迁移/部署/回滚）
  - 关键配置变更（环境变量、配置文件、密钥引用方式；禁止输出明文密钥）
  - 线上/测试环境验证结果（接口、页面、日志、指标、告警）
- 从 3 份文档里找到"上次更新时间/上次变更点"，用于增量合并（不重写整篇）

### 2) 更新《项目开发过程记录》

目标：把"问题—原因—解决—验证—复发预防"固化，下一次遇到同类问题可直接复用。

写入规则：
- 按时间追加（YYYY-MM-DD），每条记录包含：
  - 背景/触发：为什么会遇到
  - 现象：报错/异常表现
  - 根因：定位过程与结论
  - 解决：修改点（代码/配置/依赖/流程）
  - 验证：如何确认修复有效（dev/test/prod 分别说明）
  - 避坑：下次如何快速识别、如何预防
- 如果是已存在的问题类型：在原条目下追加"变体/新证据/更优解"，不要重复造条目
- 若本次由 `watch` 触发：补充"触发来源文件路径""影响模块"，帮助后续定位改动来源

### 3) 更新《项目部署操作手册》

目标：任何具备权限的同学照着做能把最终包部署到生产，并能回滚。

写入规则（尽量步骤化）：
- 前置条件：权限、账号、网络、依赖、产物位置、版本规范
- 构建产物：从源码到最终包的命令与注意点（区分 dev/test/prod）
- 配置注入：环境变量/配置中心/密钥管理（禁止把密钥写进文档）
- 发布步骤：上传/发布/切流/健康检查/回滚
- 验证与观测：日志、指标、告警、关键链路自测清单
- 常见故障与处理：部署失败、启动失败、配置错误、依赖缺失、回滚策略

### 4) 更新《项目宣讲稿》（按目标文档结构增量合并）

目标：按文档原有结构进行补全与更新，做到：
- 有步骤、可复现（新手照做能跑起来）
- 有解释、易理解（为什么这么做、踩坑点在哪里）
- 可作为论坛稿/公众号稿（结构化、可阅读、重点突出）

写入规则：
- 先读取本地《项目宣讲稿》既有结构，定位需要更新的栏目
- 将"本次增量"映射到对应栏目：新增/更新/删改都要解释动机与收益
- 优先输出"本次主题、影响模块、对外讲法"，让自动生成内容更接近正式宣讲稿
- 用"从 0 到 1"的复现路径串起来：准备 → 启动 → 核心功能演示 → 常见问题

### 5) 变更落盘与可追溯

- 每次更新都在 3 份文档顶部（或约定的变更区）写入：
  - 更新时间（含时区）
  - 变更来源（commit hash / 版本号 / 发版单号 / 需求号）
  - 本次摘要（3–7 条要点）
- 永远采用"增量合并"：只更新受影响段落，不重写整篇，不改变既有结构与风格

## 边界与约束

- 不覆盖或改写用户的 .env 文件；如必须变更配置，只给出建议与差异点
- 不在 dev/prod 环境引入假数据/桩数据；仅测试可以使用 mock（且只放在测试目录）
- 不输出任何密钥/Token 明文；文档中只写密钥来源与注入方式
- 只处理与本次开发/部署相关的内容，避免牵扯无关模块
